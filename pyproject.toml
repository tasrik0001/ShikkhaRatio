[project]
name = "teachergapmap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tasrik0001", email = "tasrik0001@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
teachergapmap = "teachergapmap:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
