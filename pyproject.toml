[project]
name = "shikkharatio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tasrik0001", email = "tasrik0001@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
]

[project.scripts]
shikkharatio = "shikkharatio:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
